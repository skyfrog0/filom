# 2026-04-21 工作日志

## 完成事项

- 提交并推送 cluster 多进程架构升级与 localStorage 持久化代码
  - commit: a34a11e
  - 包含：master.js、sessions.json、upload.html 持久化等

## 技术笔记

- 提交时排除 src/chunks/ 测试临时文件

## Bug 修复

- 修复大文件上传需要选择两次文件的 bug
  - 根因：`createTask` 中 `_needsFile: !existingUploadId`，正常选文件时 existingUploadId 为 null，导致 _needsFile=true，startTask 判断后要求用户再选一次
  - 修复：`_needsFile` 始终初始化为 false，只在 localStorage 恢复时（restoreTasks）才设为 true
  - 顺带修正：pending 状态文案改为"等待上传"；只有 waiting_file 状态才显示"选择文件"按钮
  - commit: 72d2e06，已推送
